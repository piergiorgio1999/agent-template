# ACC-29

Status: NOT RUN

Purpose: Copier inheritance of repository maps

Expected Result: disposable `standalone` and `mcp` projects receive map modes,
README marker blocks, freshness controls, and Project Status Refresh. Each
derives data only from its own HEAD and GitHub repository, contains no owner,
repository, or state value hardcoded from the template source, and never
persists live status during generation or `copier update`

Implementation: NOT IMPLEMENTED — evidence pending; no claim.

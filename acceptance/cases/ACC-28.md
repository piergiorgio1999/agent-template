# ACC-28

Status: NOT RUN

Purpose: Four-tool and read-only contract

Expected Result: exactly four custom tools remain; `project-status` exposes
map generation, `agent-config-check` checks static freshness, and GitHub access
is read-only. No LLM generator, remote service, webhook, write-back, new source
of truth, authoritative map, or file forbidden by SPEC section 2 exists; maps
do not replace their canonical code, manifest, workflow, GitHub, decision, or
Digest sources

Implementation: NOT IMPLEMENTED — evidence pending; no claim.

# ACC-24

Status: NOT RUN

Purpose: Recursive repository map views and admitted sources

Expected Result: `dependencies`, `ci`, and `status` traverse their admitted
sources from root to leaf; fixtures exercise at least four levels, stable
references, and cycle closure. Dependency fixtures cover classified
components, all four supported `package.json` dependency groups,
`Package.resolved` pins, and explicit `details unavailable` for each recognized
unsupported manifest. CI is identified as the section 9 contract view, never
as an extracted workflow DAG. No node or relation comes from LLM inference or
a presumed semantic relationship

Implementation: NOT IMPLEMENTED — evidence pending; no claim.

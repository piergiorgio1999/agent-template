# ACC-26

Status: NOT RUN

Purpose: Static and live map separation

Expected Result: README persists only `dependencies` and `ci`; `status` uses
the Project Status Digest categories, precedence, ordering, and limits and is
published only as a read-only per-run snapshot; README contains its documented
command and Actions link. Project Status Refresh covers push, branch
create/delete, manual dispatch, all contracted PR and Issue/label/milestone
events, and completed CI; it publishes all three previews, leaves the worktree
unchanged, and creates no committed state or authoritative cache

Implementation: NOT IMPLEMENTED — evidence pending; no claim.

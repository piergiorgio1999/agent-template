# Decisions

## V0.1

- GitHub Issues, PR e Checks sono le fonti operative.
- `scope-map.json` è l'unica fonte per la classificazione dei file.
- Il CI espone un solo check required: `CI Gate`.
- Il custom code è limitato a `tools/project-status`, `tools/scope-guard` e
  `tools/agent-config-check`.


# MCP integration profile

This project was generated with the `mcp` integration profile.

The profile selects MCP-specific integration points at generation time. The
shared repository checkers and release gate remain authoritative; an MCP
orchestrator must invoke them and stop on any non-zero exit status.

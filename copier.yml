_min_copier_version: "9.0.0"
_exclude:
  - .git
  - .git/**
  - copier.yml
  - template-only
  - template-only/**
project_name:
  type: str
  default: agent-project
template_owner:
  type: str
  default: OWNER
default_branch:
  type: str
  default: main

# Claude project instructions

Use `AGENTS.md` as the canonical project instruction file.

Do not create `STATUS.md`, `ROADMAP.md`, `TASKS.md`, `PLAN.md`, or
`PROJECT_STATE.json`. Keep changes within the declared scope in
`scope-map.json` and use GitHub Issues, PRs, and CI Checks as the project
record.

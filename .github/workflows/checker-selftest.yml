name: Checker Self Test

on:

  workflow_dispatch:

  schedule:
    - cron: '0 3 * * 1'

jobs:

  selftest:

    runs-on: ubuntu-latest

    steps:

      - uses: actions/checkout@v4

      - run: |
          echo "TODO selftest"
